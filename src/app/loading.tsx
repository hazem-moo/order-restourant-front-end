import Logo from "@/components/Logo";
import React from "react";

const Loading = () => {
  return (
    <div className="loader">
      <Logo />
      <h1>loading page...</h1>
    </div>
  );
};

export default Loading;
